export enum DeliveryStep {
    VALIDATION_PENDING,
    IN_PREPARATION,
    READY_FOR_PICKUP,
    ON_THE_WAY,
    AT_YOUR_DOOR,
    DELIVRED,
}

export enum LineStep {
    EMPTY,
    HALF,
    COMPLETE,
}
